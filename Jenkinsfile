pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                // Checkout the code from your repository
                git 'https://github.com/jefrey007/my-maven-app.git'
            }
        }
    }
}
